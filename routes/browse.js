const express = require("express");
const browseController = require("../controllers/browse");

const router = express.Router();

//router.'method'('url', browseController.'modulename');

module.exports = router;
