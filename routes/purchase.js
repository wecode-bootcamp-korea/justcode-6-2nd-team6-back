const express = require("express");
const purchaseController = require("../controllers/purchase");

const router = express.Router();

// router.'method'('url', purchaseController.'modulename');

module.exports = router;
