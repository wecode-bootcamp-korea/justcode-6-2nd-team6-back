const express = require("express");
const detailController = require("../controllers/detail");

const router = express.Router();

//router.'method'('url', detailController.'modulename');

module.exports = router;
